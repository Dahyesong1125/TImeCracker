import javax.sound.sampled.Line;

public class App {
  public static void main(String[] args) throws Exception {
    Line linel = new Line(
        new Point(x:1, y:2),
        new Point(x:3, y:4)
      );
    Line line = new Line(x:10,y1:20, x2:30, y2:40);
    

    System.out.println(line1.toString());

    Triangle tri1 = new Triangle(null, null, null)
  }
}
